<!-- source-hash: e8b7e739422bed4f6c548852f43d684d -->
Emscripten-compiled Run-Length Encoding (RLE) module that provides a cross-environment JavaScript wrapper for native RLE compression/decompression logic, supporting Node.js, browser, web worker, and shell runtimes.

## Key Components

- **`Module`** — Central interface object exposing all functionality; handles environment detection and capability bridging
- **`Runtime`** — Low-level memory and type utilities ported from C/C++ semantics, including:
  - `stackSave` / `stackRestore` — Stack pointer management
  - `forceAlign` — Memory alignment calculations
  - `getNativeTypeSize` / `getNativeFieldSize` — C type size resolution (`i1`–`i64`, `float`, `double`)
  - `calculateStructAlignment` — C struct layout computation with padding
  - `or64` / `and64` / `xor64` — 64-bit bitwise operations via split hi/lo arithmetic
  - `dedup` — Array deduplication utility
- **Environment guards** — `ENVIRONMENT_IS_NODE`, `ENVIRONMENT_IS_WEB`, `ENVIRONMENT_IS_WORKER`, `ENVIRONMENT_IS_SHELL` flags that configure `Module['read']`, `Module['print']`, and `Module['load']` per runtime

## Usage Example

```javascript
// Node.js usage
const Module = require('./rle.js');

// Module exposes read/load helpers automatically configured for Node
const data = Module['read']('input.bin', true); // binary read

// Override print before loading
Module['print'] = (msg) => console.info('[RLE]', msg);
Module['printErr'] = (msg) => console.error('[RLE ERROR]', msg);

// Hooks run before/after compiled code executes
Module['preRun'].push(() => { /* setup */ });
Module['postRun'].push(() => { /* teardown */ });
```

## Notes

This is auto-generated Emscripten output — direct editing is not recommended. The file is part of MeshCentral's remote desktop stack, likely handling RLE-compressed screen data for the KVM/remote control subsystem.

**Source:** [`rle.js`](https://github.com/flamingo-stack/meshcentral/blob/main/rle.js)